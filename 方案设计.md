# 方案设计：基于 Hy3 的「论文结构化阅读助手」与自定义评估方法

> 非腾讯官方项目。应用基于 Hy3（腾讯混元 HY-3，OpenAI 兼容端点）调用构建，无训练 / 微调。
> 本仓库为完整开源作品，含应用源码、评测脚本、样本集、验证实验与本方案文档。

本方案覆盖五个要素：**设计思路、架构、重点技术、预期效果、时间规划**。

---

## 1. 设计思路

### 1.1 场景选择
任务书给出六个可选方向（法律 / 医疗 / 金融 / 学术 / 内容生产 / 复合型产物）。本项目选择 **学术方向 → 论文阅读助手（结构化笔记生成）**，理由：

- **真实用户与高频痛点**：本科生、研究生、算法工程师在文献调研时，常面对长而密集的论文原文，需要快速抓住「目标—方法—创新—结果—不足」五条主线；逐篇精读成本高、跨领域术语门槛高。
- **大模型必要性充分**：该任务属典型的「长文本理解 + 可控格式生成」，需保留专业术语、做结构化抽取、并保持事实一致性——正是 LLM 擅长、而传统规则 / 关键词方法难以胜任的场景。
- **输出无唯一标准答案**：同一篇论文的「好笔记」可有多重合理写法（详略、组织、术语取舍不同），因此**必须设计自定义评估标准**而非比对标准答案，契合任务书「开放式场景」要求。
- **评估可操作**：论文原文即「事实基准」，使「事实准确性 / 可追溯性」等维度具备可核对的客观锚点，便于设计可复现的评判标准。

### 1.2 总体设计哲学
- **应用侧**：把"读论文"这一开放任务收敛为"固定 5 板块结构化笔记"，用强约束 prompt 提升可控性与可评测性。
- **评估侧**：采用**规则 + LLM-as-judge 混合**范式——客观可程序化校验的维度交给规则（零成本、可复现、可解释），需语义理解的主观维度交给 LLM 评审，二者互补并形成可解释证据链。
- **健壮性**：全程仅依赖 Python 标准库（urllib / json / re），并在无 API key 时提供 `rule_fallback` 降级模式，保证仓库可离线演示、可移植。

### 1.3 风险意识（决定评估重点）
论文阅读助手的最大风险是 **幻觉（编造方法 / 数值 / 结论）**。因此评估维度权重向"事实准确性（0.25）"与"可追溯性（0.20）"倾斜，并把"伪造引用检测"作为对抗验证的核心探针。

---

## 2. 架构

### 2.1 模块划分
| 模块 | 文件 | 职责 |
|---|---|---|
| 模型客户端 | `src/client.py` | 封装 Hy3 OpenAI 兼容接口（urllib 实现），含重试、超时、JSON 容错提取 |
| 应用侧 | `src/app.py` | 定义 5 板块生成 prompt，调用客户端产出结构化笔记 |
| 评估核心 | `src/rubric.py` | 6 维定义、0–5 可操作锚点、权重、整体分与等级映射 |
| 混合评估器 | `src/evaluator.py` | 规则评分 + LLM-as-judge + 降级模式 + 伪造引用检测 |
| 评测编排 | `eval/run_eval.py` | 对样本集逐篇生成→评估→落盘，汇总结果表 |
| 验证实验 | `eval/validate_discrimination.py` / `validate_consistency.py` / `validate_adversarial.py` | 判别力 / 一致性 / 对抗鲁棒性验证 |
| 样本与产出 | `dataset/` `output/` `results/` | 输入论文、生成笔记、评测结果 |

### 2.2 数据流图
```
┌──────────────┐
│ 论文原文      │  dataset/papers/*.txt
│ (.txt)       │
└──────┬───────┘
       │
       ▼
┌─────────────────────────────┐
│ src/app.py  generate_note()  │
│  system: 5 板块结构化指令      │
│  (禁止编造 / 保留术语 / 不展开思考) │
└──────┬──────────────────────┘
       │  HTTP POST (urllib, OpenAI 兼容 /chat/completions)
       ▼
┌──────────────────────┐   reasoning_content / content
│ Hy3 (HY-3) 推理模型    │◄──────────────────────────┐
│ tokenhub.tencentmaas  │                           │
└──────────┬───────────┘                           │
           │                                       │
           │ 重试：content 为空时 max_tokens 翻倍    │
           ▼ 直到拿到非空 content                    │
┌─────────────────────────────┐                    │
│ 结构化笔记 (output/note_*.md) │                    │
└──────┬──────────────────────┘                    │
       │                                           │
       ▼                                           │
┌──────────────────────────────────────────────────┐
│ src/evaluator.py  evaluate()                       │
│  ├─ 规则层(锚点): 格式 / 完整度 / 可追溯性          │
│  └─ LLM-as-judge : 事实 / 术语 / 可读性 ───────────┘
│      (无 key 时 rule_fallback 降级)                 │
└──────┬─────────────────────────────────────────────┘
       ▼
┌─────────────────────────────────────┐
│ 6 维 0–5 分 + 加权整体分(0–5) + 等级   │
│ results/result_table.csv / summary.json │
│ results/case_analysis.md               │
└─────────────────────────────────────┘

批处理：eval/run_eval.py 对 dataset/papers/ 下每篇论文循环执行
         "生成 → 评估 → 落盘"，最终汇总为 results/ 下的表格与归因。
```

### 2.3 评估子架构（混合范式）
- **规则层（deterministic）**：`format`（5 标题 + 分点检测）、`completeness`（板块命中数 + 子要点校验）、`traceability`（论断句与原文的信号重叠率 `trace_ratio`）。产出可解释证据（命中了哪些标题、重叠率多少），便于失败归因。
- **LLM-as-judge 层**：`factual_accuracy`、`terminology`、`readability` 由 Hy3 对照原文打分（同样给出 0–5 可复现锚点）。
- **降级层**：无 API key 或 LLM 调用失败时，`rule_fallback` 用规则近似事实 / 术语分，并在结果中显式标注 `mode='rule_fallback'`，明确其仅为离线演示。

---

## 3. 重点技术

### 3.1 Hy3 推理模型的适配（关键坑点）
Hy3 是**推理模型**：常把 token 预算消耗在 `reasoning_content` 上，导致 `choices[0].message.content` 为空（HTTP 200 但无正文），并伴随 `finish_reason: length`。
- 客户端在检测到 `content` 为空时，按「token 受限」处理，**每次重试将 `max_tokens` 翻倍**（默认 2048→4096→8192…），直到拿到非空正文。
- 超时设为 300s，并带 4 次指数退避重试，覆盖网络抖动与限流。
- 生成 prompt 显式要求"直接输出最终笔记、不展示思考过程"，进一步压缩推理预算占用。

### 3.2 零依赖客户端（可移植性）
仅用 `urllib.request` 发起 OpenAI 兼容请求，不依赖 `openai` / `requests` / `python-dotenv`；`.env` 用自写的极简解析器读取（`API_KEY` / `BASE_URL` / `MODEL_NAME`）。这让仓库可在受限 / 离线环境直接运行。

### 3.3 Prompt 工程（可控生成）
- 5 个固定一级标题板块：`研究目标 / 核心方法 / 主要创新点 / 实验结果与分析 / 存在不足与未来方向`。
- 硬性规则：① 所有内容须严格来自原文，禁止编造；② 保留关键术语与缩写（首次出现给全称）；③ 信息不足写"原文未充分提供"而非臆造；④ 推断内容须标注「（推断）」。
- 输出约束：直接返回笔记正文，不输出解释性前缀，降低后处理成本。

### 3.4 可追溯性度量（启发式）
对笔记中**含具体信号（数字 / 长度≥3 的拉丁术语）的论断句**，计算其信号在原文中的出现比例，取全笔记均值得到 `trace_ratio`：
- 仅校验含事实信号的句子，纯中文阐述句视为中性、不计入，避免对合理概括过度惩罚；
- 含伪造数字 / 术语的句子，其信号在原文缺失 → 重叠率下降 → 被识别。

### 3.5 伪造引用检测（对抗探针）
正则抽取笔记中的引用样式（`[12]`、`arXiv:2005.12872`、`Author et al.`），若其在原文中找不到，则列入"疑似伪造引用"，作为幻觉的强信号。

### 3.6 加权评估与等级
整体分 = Σ(维度分 × 权重)，权重：`事实准确性 0.25`、`可追溯性 0.20`、`完整度 0.15`、`术语 0.15`、`可读性 0.15`、`格式 0.10`（Σ=1.0）。整体分映射等级：≥4.5 优(A)、≥3.5 良(B)、≥2.5 中(C)、≥1.5 差(D)、其余 劣(F)。

---

## 4. 预期效果

### 4.1 应用效果预期
| 指标 | 目标 | 实测（7 篇跨领域论文） |
|---|---|---|
| 5 板块完整率 | 100% | 100%（7/7 全板块命中） |
| 整体评分等级 | 优 (A) 为主 | 全部 优 (A)，整体分 4.6–5.0 |
| 幻觉（伪造数值/结论） | 接近 0 | 正式评测未出现；对抗样本可被检测 |

### 4.2 评估工具有效性预期（验证目标）
| 验证项 | 方法 | 目标 / 实测 |
|---|---|---|
| 判别力 | 对 good/medium/bad 候选自动打分，与专家排序算 Spearman | 目标 ≥0.7；实测 **0.886**（单调递增 good>medium>bad） |
| 一致性 | 每候选重复评测 5 次，与专家标注算 Spearman + 标准差 | 目标 Spearman ≥0.7 且 std 小；实测 **0.814**，std≈0 |
| 对抗鲁棒性 | 注水(padding)/术语堆砌(jargon)/伪造引用(fake-cite) 三类对抗样本 | 目标全 PASS；实测 **三项均 PASS**（伪造引用被捕获） |

> 详细实验过程与数据见 `results/validation_discrimination.json`、`validation_consistency.json`、`validation_adversarial.json` 与 `results/case_analysis.md`；结论与失败模式见 `report/analysis_report.md`。

### 4.3 失败模式与边界（已知）
- `completeness` 对"模糊但无误"的笔记偏宽松；
- 伪造引用会被捕获，但因权重结构整体仍可能落在"良"，需在报告层重点提示；
- `traceability` 为启发式代理，无法识别"语义改写后事实错误但字面命中"的情况；
- `readability` 评委偏宽松，宜作为基线维度而非否决项。

---

## 5. 时间规划

以下为项目里程碑规划（按阶段拆分，便于复现与评审）。括号内为实际完成情况。

| 阶段 | 时间窗 | 目标交付物 | 状态 |
|---|---|---|---|
| **P0 需求与基线** | 第 1 天 | 场景选定、5 板块产出约定、`.env` 配置样例、只读标准库客户端打通 Hy3 接口 | ✅ 已完成 |
| **P1 应用开发** | 第 2 天 | `src/client.py` + `src/app.py`，单篇论文可生成结构化笔记；修复 Hy3 空 content 重试 | ✅ 已完成 |
| **P2 评估方法** | 第 3–4 天 | `src/rubric.py`（6 维 0–5 锚点 + 权重）、`src/evaluator.py`（规则 + LLM-judge + 降级）、`eval/METHOD.md` | ✅ 已完成 |
| **P3 样本与评测** | 第 5 天 | `dataset/papers/`（7 篇跨领域：CV/NLP/医学/金融/检测）、`dataset/candidates/`（好/中/差 + 3 对抗）、`eval/run_eval.py`、`dataset/sample_set.md` | ✅ 已完成 |
| **P4 验证实验** | 第 6 天 | 判别力 / 一致性 / 对抗鲁棒性三套验证脚本与 JSON 结果、图表 | ✅ 已完成 |
| **P5 分析与交付** | 第 7 天 | `report/analysis_report.md`（场景/方案/维度依据/结论/失败模式/典型模式）、`demo`（<2min GIF+MP4）、`README.md`、`交付清单.md`、整理为开源仓库 | ✅ 已完成 |
| **P6 开源发布** | 第 7 天+ | 推送到 GitHub（远程 `origin/main` 已配置） | 
### 关键里程碑节点
- **M1（P1 结束）**：能端到端生成一篇可评测的结构化笔记。
- **M2（P2 结束）**：评估器对"好/坏笔记"能稳定区分，并输出可解释证据。
- **M3（P3 结束）**：7 篇真实评测全部达到优(A)，证明方案在跨领域有效。
- **M4（P4 结束）**：三套验证证明评估工具自身可信（判别力/一致性/对抗均达标）。
- **M5（P5 结束）**：完整交付物齐备，仓库结构清晰、密钥不入库。
