# 交付清单（对照任务书要求）

> 本仓库根目录即为「犀牛鸟实战任务」完整提交包；以下逐条对照任务书 5 大类要求。
> 应用基于 Hy3（腾讯混元 HY-3）构建，仅依赖 Python 标准库，无需训练/微调。

## 一、开源项目仓库
| 内容 | 位置 |
|---|---|
| 应用源码（Hy3 客户端 + 论文阅读助手） | `src/client.py`、`src/app.py` |
| 评估方法（rubric + 混合评估器） | `src/rubric.py`、`src/evaluator.py` |
| README（场景/结构/运行说明） | `README.md` |
| 环境配置样例 | `config.example.env`（复制为 `.env` 填密钥；**真实密钥不入库**） |
| 依赖说明 | `requirements.txt`（标准库即可，Python≥3.8） |

## 二、评测材料
| 内容 | 位置 |
|---|---|
| 评测样本集（**7 篇**跨领域输入论文：CV×4 含新增 YOLO26 / DenseDETR / 航拍小目标检测、NLP、医学、金融；+ 好/中/差/3 对抗候选） | `dataset/papers/`、`dataset/candidates/` |
| 评测样本集说明（来源/构造/覆盖/难例反例占比） | `dataset/sample_set.md` |
| 评估方法说明文档（6 维可操作锚点 + 权重 + 规则/LLM-judge 选型依据 + 验证设计） | `src/rubric.py`、`eval/METHOD.md`、`report/analysis_report.md` 第 3 节 |
| 评测脚本（自动/半自动） | `eval/run_eval.py`、`eval/validate_*.py` |
| 完整结果表格 | `results/result_table.csv`、`results/summary.json` |
| 专家参考标注（人工基准） | `eval/human_labels.json` |

## 三、有效性验证结果（真实运行）
| 验证项 | 结果文件 | 关键结论 |
|---|---|---|
| 判别力 | `results/validation_discrimination.json` | Spearman=**0.886**，good>medium>bad 单调成立 |
| 一致性（稳定性+与人工） | `results/validation_consistency.json` | 5 次重复 std 多为 0；与专家 Spearman=**0.814** |
| 对抗性 | `results/validation_adversarial.json` | 堆篇幅/堆术语/伪造引用 **三项均被识别** |
| 结果图表 | `results/chart_overall.svg`、`results/chart_mae.svg` | — |

## 四、分析报告
| 内容 | 位置 |
|---|---|
| 场景选择理由 / 解决方案 / 维度设计依据 / 评测结论 / 失败模式与能力边界 / 典型模式 | `report/analysis_report.md` |
| 典型 Case 归因 | `results/case_analysis.md` |

## 五、Demo（≤ 2 分钟）
| 内容 | 位置 |
|---|---|
| 演示视频（约 56 秒，H.264 MP4；标题→痛点→终端演示→6 维评估→结果→结尾） | `demo/demo.mp4` |
| 动画 GIF（循环，单轮约 5 秒，Pillow 生成） | `demo/demo.gif` |
| 可交互演示页（含视频与 GIF） | `demo/demo.html` |
| 视频生成脚本 | `demo/make_demo_video.py` |
| GIF 生成脚本 | `demo/make_demo_gif.py` |

## 运行顺序（在仓库根目录）
```bash
cp config.example.env .env      # 填入你的 Hy3 API_KEY / BASE_URL / MODEL_NAME
python eval/run_eval.py                 # 完整评测
python eval/validate_discrimination.py  # 判别力
python eval/validate_consistency.py     # 一致性
python eval/validate_adversarial.py     # 对抗性
python eval/make_charts.py              # 生成图表
```
无密钥时评测自动降级为 `rule_fallback` 模式，流程仍可演示（结果会明确标注）。
